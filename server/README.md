# Auto USB/IP — Server Daemon

This directory contains the lightweight background daemon (`autousbip.py`) and systemd service file for the remote USB host (e.g. Raspberry Pi or Linux server).

Please see the main [Project README](../README.md) at the root of the repository for full installation and systemd deployment instructions.
